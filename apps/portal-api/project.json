{
  "name": "portal-api",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/portal-api",
  "projectType": "application",
  "tags": ["type:api", "scope:portal"],
  "targets": {
    "build": {
      "executor": "@nx-dotnet/core:build",
      "outputs": ["{workspaceRoot}/dist/apps/portal-api", "{workspaceRoot}/dist/intermediates/apps/portal-api"],
      "options": {
        "configuration": "Debug",
        "noDependencies": true
      },
      "configurations": {
        "production": {
          "configuration": "Release"
        }
      }
    },
    "serve": {
      "executor": "@nx-dotnet/core:serve",
      "options": {
        "launchProfile": "https"
      }
    }
  }
}
